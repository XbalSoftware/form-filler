//
//  AppLinks.swift
//  Form Filler
//
//  External links shown on the About page. The documents live in the
//  repo under docs/ — publish them (e.g. GitHub Pages) and update these
//  URLs to the published location.
//
//  TODO(user): replace the placeholder host with the real GitHub Pages
//  site once it exists.
//

import Foundation

nonisolated enum AppLinks {
    static let userManual = URL(string: "https://example.github.io/form-filler/user-manual")!
    static let privacyPolicy = URL(string: "https://example.github.io/form-filler/privacy-policy")!
}
